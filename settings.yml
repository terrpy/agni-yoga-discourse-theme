# Agni Yoga World Community - Theme Settings
# Color values are defined directly in common/common.scss
# Add any configurable settings here using supported types:
#   integer, bool, list, enum, upload
#
